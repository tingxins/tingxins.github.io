# Migration Notes

This folder records compatibility checks for the Jekyll to Astro migration.

- `asset-mapping.md` tracks historical `http://image.tingxins.cn` references and local replacements.
- Build verification should check old post URLs, `/feed.xml`, `/privacy/iRepeater_privacy.html`, and `/assets/...`.
